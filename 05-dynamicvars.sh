#!bin/bash
### There are some variables, whose values gets changed withrespect to time. These are called as "Dynamic variables".
### These should not be hardcoded as their data/value gets changed too often. So,instead we should softcode them using expressions to dynamically represent the results.
# For example,
echo "\e[32m Today's date is 22 Nov 2021 \e[0m"
 